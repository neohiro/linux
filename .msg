pass 9 correctness: lib/temp.sh ERR trap removed -- it aborted on the first failed sub-command in STRICT_RUN=1 mode, breaking the contract that run() collects _FAIL_COUNT and reports at end-of-run. Cleanup now happens only via EXIT trap. _log_error() retained for explicit failure logging. wired into run() via declare -F guard so it works whether lib/temp.sh was sourced or the inline fallback ran. robustness: DeepClean.sh ${USE_COLOR} now uses ${USE_COLOR:-0} (set -u at line 7 was binding the variable to unbound, would have errored when lib/color.sh was missing).
