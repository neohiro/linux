pass 7 correctness: fix two raw ANSI escapes that bypassed _c color-gating on TERM=dumb (Tailscale SSH). KERNEL REBOOT banner and run_remote_script paste divider now use _c so they print as plain text on dumb terminals instead of literal CSI escapes. robustness: gpg_out made local in _verify_remote_gpg_signature (was non-local and tracked via _tmpfile for non-sensitive output; now uses plain mktemp with explicit rm -f).
