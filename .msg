pass 10 robustness: _log_error now takes explicit <rc> <cmd> args instead of relying on $BASH_COMMAND. $BASH_COMMAND is the currently-executing function name when called from run(), not the failed command -- breadcrumbs were useless. run() passes "$*" as the command string so debug log entries show the actual command that failed.
